---
repository: https://github.com/bash-it/bash-it.git
version: master
theme: pure
aliases:
  - general
plugins:
  - base
  - alias-completion
completions:
  - bash-it
  - system
user: "{{ ansible_user_id }}"
